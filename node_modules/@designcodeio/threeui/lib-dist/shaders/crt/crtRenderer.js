import { CRT_VERTEX_SHADER as oe, CRT_FRAGMENT_SHADER as ae } from "./crtShaders.js";
const ie = { speed: 1, typeSpeed: 1, motion: 1, brightness: 1, opacity: 1, hue: 0, saturation: 1 }, t = (o, s = "p") => ({ t: o, c: s }), c = (o) => "·".repeat(o), _ = [
  [t("ZION MAINFRAME  v9.1.1"), t("   (c) 2199 Nebuchadnezzar", "d")],
  [t("CONSTRUCT Broadcast  Rev M  S/N NX-0101-0011", "d")],
  [],
  [t("Hacking Matrix grid nodes "), t(`${c(14)} `, "d"), t("OK", "a")],
  [t("Neural Jack  0x000-0x0FF "), t(`${c(11)} `, "d"), t("ONLINE "), t("OK", "a")],
  [t("Pinging agent signatures "), t(`${c(6)} `, "d"), t("3 found")],
  [t("nav0  OPERATOR UPLINK SECURE ", "d"), t(`${c(6)} `, "d"), t("READY", "a")],
  [t("vis0  CODE RAIN DECRYPT 256bit ", "d"), t("READY", "a")],
  [t("net0  HARDLINE CONNECTION MAX ", "d"), t(`${c(4)} `, "d"), t("LINK", "a")],
  [t("red0  RED PILL EXTRACTION ", "d"), t(`${c(4)} `, "d"), t("READY", "a")],
  [t("Mounting /dev/mind -> ROOT: "), t(`${c(6)} `, "d"), t("OK", "a")],
  [t("Loading weapon training program "), t(`${c(4)} `, "d"), t("OK", "a")],
  [t("Starting [ jmp spd str wpn ] "), t(`${c(4)} `, "d"), t("OK", "a")],
  [t("Locating the Oracle sector "), t(`${c(6)} `, "d"), t("99.9%")],
  [],
  [t("SYSTEM ANOMALY  "), t("detected.", "h")],
  [t("subject Thomas A. Anderson   status asleep ", "d"), t("z", "d"), t("Z", "d")],
  [],
  [t("wake up: ")]
], k = { p: { fill: "#8df0b4", glow: "rgba(28,236,132,0.95)" }, d: { fill: "#4f9a76", glow: "rgba(28,236,132,0.45)" }, a: { fill: "#ffba5e", glow: "rgba(255,150,52,0.95)" }, h: { fill: "#eafff3", glow: "rgba(120,255,190,0.95)" } }, B = (o) => o.reduce((s, R) => s + R.t.length, 0), H = _.reduce((o, s) => o + B(s), 0), F = Math.max(..._.map(B));
function K(o, s, R) {
  const e = o.createShader(s);
  if (!e) throw new Error("Unable to create CRT shader");
  if (o.shaderSource(e, R), o.compileShader(e), !o.getShaderParameter(e, o.COMPILE_STATUS)) throw new Error(o.getShaderInfoLog(e) ?? "CRT shader compilation failed");
  return e;
}
function le(o, s, R) {
  const e = s.getContext("webgl", { antialias: !1, alpha: !1, depth: !1, premultipliedAlpha: !1 });
  if (!e) throw new Error("CRT requires WebGL");
  const p = document.createElement("canvas"), a = p.getContext("2d");
  if (!a) throw new Error("CRT text canvas unavailable");
  const X = K(e, e.VERTEX_SHADER, oe), G = K(e, e.FRAGMENT_SHADER, ae), i = e.createProgram();
  if (!i) throw new Error("Unable to create CRT program");
  if (e.attachShader(i, X), e.attachShader(i, G), e.linkProgram(i), !e.getProgramParameter(i, e.LINK_STATUS)) throw new Error(e.getProgramInfoLog(i) ?? "CRT link failed");
  e.useProgram(i);
  const Y = e.createBuffer();
  e.bindBuffer(e.ARRAY_BUFFER, Y), e.bufferData(e.ARRAY_BUFFER, new Float32Array([-1, -1, 3, -1, -1, 3]), e.STATIC_DRAW);
  const v = e.getAttribLocation(i, "aPos");
  e.enableVertexAttribArray(v), e.vertexAttribPointer(v, 2, e.FLOAT, !1, 0, 0);
  const W = e.getUniformLocation(i, "uTex"), z = e.getUniformLocation(i, "uRes"), J = e.getUniformLocation(i, "uTime"), V = e.getUniformLocation(i, "uMotion"), w = e.createTexture();
  e.bindTexture(e.TEXTURE_2D, w), e.texParameteri(e.TEXTURE_2D, e.TEXTURE_MIN_FILTER, e.LINEAR), e.texParameteri(e.TEXTURE_2D, e.TEXTURE_MAG_FILTER, e.LINEAR), e.texParameteri(e.TEXTURE_2D, e.TEXTURE_WRAP_S, e.CLAMP_TO_EDGE), e.texParameteri(e.TEXTURE_2D, e.TEXTURE_WRAP_T, e.CLAMP_TO_EDGE), e.uniform1i(W, 0);
  let T = 1, x = 1, h = 14, C = 20, L = 0, g = 8, b = 0, I = 0, S = 0, m = !1, N = !0, P = 0, M = -1, A = -1;
  const $ = performance.now(), j = () => {
    L = x * 0.135, C = x * 0.74 / _.length, h = Math.max(5, Math.min(C * 0.8, T * 0.88 / (Math.max(F, 1) * 0.62))), a.font = `600 ${h.toFixed(2)}px ui-monospace, "SF Mono", "JetBrains Mono", Menlo, Consolas, monospace`, g = a.measureText("M").width || h * 0.6;
  }, Z = (n) => {
    const r = k[n];
    a.fillStyle = r.fill, a.shadowColor = r.glow, a.shadowBlur = h * 0.55;
  }, q = (n) => {
    a.setTransform(1, 0, 0, 1, 0, 0), a.fillStyle = "#03100a", a.fillRect(0, 0, T, x), a.textBaseline = "top", a.font = `600 ${h.toFixed(2)}px ui-monospace, "SF Mono", "JetBrains Mono", Menlo, Consolas, monospace`;
    let r = n, f = L;
    b = Math.floor((T - F * g) / 2), I = L;
    for (const E of _) {
      const d = B(E), l = n === 1 / 0 ? 1 / 0 : Math.min(r, d);
      let U = Math.floor((T - F * g) / 2), D = 0;
      for (const O of E) {
        let u = O.t;
        if (l !== 1 / 0) {
          const y = l - D;
          if (y <= 0) break;
          y < u.length && (u = u.slice(0, y));
        }
        if (u.length && (Z(O.c), a.fillText(u, U, f), U += g * u.length), D += O.t.length, l !== 1 / 0 && D >= l) break;
      }
      if (b = U, I = f, l !== 1 / 0 && (r -= l), f += C, l !== 1 / 0 && r <= 0) break;
    }
  }, Q = () => {
    a.shadowColor = k.p.glow, a.shadowBlur = h * 0.6, a.fillStyle = "#bdf8d2", a.fillRect(b, I + h * 0.06, Math.max(g * 0.92, 4), h * 0.96);
  }, ee = () => {
    e.bindTexture(e.TEXTURE_2D, w), e.pixelStorei(e.UNPACK_FLIP_Y_WEBGL, !0), e.texImage2D(e.TEXTURE_2D, 0, e.RGBA, e.RGBA, e.UNSIGNED_BYTE, p), e.pixelStorei(e.UNPACK_FLIP_Y_WEBGL, !1), N = !1;
  }, te = () => {
    const n = o.getBoundingClientRect(), r = Math.max(1, n.width), f = Math.max(1, n.height), E = r < 700 ? 0.82 : 0.55, d = Math.min(Math.round(r * E), 920), l = Math.round(d * f / r);
    (s.width !== d || s.height !== l || T !== d) && (s.width = d, s.height = l, p.width = d, p.height = l, T = d, x = l, j(), M = -1, A = -1), e.viewport(0, 0, d, l), e.uniform2f(z, d, l);
  }, re = (n) => {
    const r = m ? 1 / 0 : Math.floor(S), f = Math.floor((n - $) / 420) % 2 === 0 ? 1 : 0, E = m ? f !== A : n - P > 42;
    r === M && f === A && !E || !m && n - P <= 42 && r === M && f === A || (q(r), f && Q(), P = n, M = r, A = f, N = !0);
  };
  return { resize: te, render(n) {
    const r = R();
    m || (S += 4.4 * r.typeSpeed, S >= H && (S = H, m = !0)), re(n), N && ee(), e.useProgram(i), e.uniform1f(J, (n - $) * 1e-3 * r.speed), e.uniform1f(V, r.motion), e.drawArrays(e.TRIANGLES, 0, 3);
  }, dispose() {
    e.deleteBuffer(Y), e.deleteTexture(w), e.deleteProgram(i), e.deleteShader(X), e.deleteShader(G);
  } };
}
export {
  ie as CRT_DEFAULTS,
  le as createCrtRenderer
};
