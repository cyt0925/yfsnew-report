import { jsx as p } from "react/jsx-runtime";
import { useRef as a, useEffect as R } from "react";
import { CRT_DEFAULTS as g, createCrtRenderer as A } from "./crtRenderer.js";
function z({ className: u = "", ...f }) {
  const m = a(null), d = a(null), s = a({ ...g, ...f });
  s.current = { ...g, ...f }, R(() => {
    const n = m.current, l = d.current;
    if (!n || !l) return;
    const t = A(n, l, () => s.current);
    let e = 0, i = !0;
    const v = () => {
      t.resize(), t.render(performance.now());
    }, o = (c) => {
      t.render(c), e = i && !document.hidden ? requestAnimationFrame(o) : 0;
    }, b = new ResizeObserver(v), h = new IntersectionObserver(([c]) => {
      i = c?.isIntersecting ?? !0, i && !e && (e = requestAnimationFrame(o)), !i && e && (cancelAnimationFrame(e), e = 0);
    });
    return b.observe(n), h.observe(n), v(), e = requestAnimationFrame(o), () => {
      e && cancelAnimationFrame(e), b.disconnect(), h.disconnect(), t.dispose();
    };
  }, []);
  const r = s.current;
  return /* @__PURE__ */ p("div", { ref: m, className: `threeui-background crt${u ? ` ${u}` : ""}`, style: { background: "#03100a", opacity: r.opacity, filter: `hue-rotate(${r.hue}deg) saturate(${r.saturation}) brightness(${r.brightness})` }, children: /* @__PURE__ */ p("canvas", { ref: d }) });
}
export {
  z as CrtBackground
};
